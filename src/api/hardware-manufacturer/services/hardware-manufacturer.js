'use strict';

/**
 * hardware-manufacturer service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::hardware-manufacturer.hardware-manufacturer');
