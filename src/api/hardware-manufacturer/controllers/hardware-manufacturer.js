'use strict';

/**
 *  hardware-manufacturer controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::hardware-manufacturer.hardware-manufacturer');
